function sayHello(name:string, age:number) {
    // console.log("Hello, "+name+"!"); // the normal way to do it
    console.log( `Hello ${name}, your age is ${age}`);
}

let user = "a Great Hello Kurt Wonnegut";
sayHello(user, 34);


export{};